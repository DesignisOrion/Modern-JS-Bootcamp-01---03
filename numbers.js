let age = 35;
let dogYears = (age + 1) / 7;

console.log(dogYears);

// challenge area

// studentScore
// maxScore
// percent

// studentScore / maxScore * 100 is the formula.

// print the percent

let studentScore = 18;
let maxScore = 20;
percent = (studentScore / maxScore) * 100;

console.log(percent);
